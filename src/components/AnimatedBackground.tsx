// AnimatedBackground is intentionally empty.
// The site uses a global CSS dot-grid pattern plus fixed ambient
// gradient orbs defined in page.tsx instead of per-section blobs.
export default function AnimatedBackground() {
    return null;
}
